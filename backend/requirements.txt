# FastAPI core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database - SQLModel (SQLAlchemy + Pydantic), async Postgres driver
sqlmodel>=0.0.19
asyncpg>=0.29.0

# Migrations
alembic>=1.13.0

# Auth - JWT tokens + password hashing
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Config & env
python-dotenv>=1.0.0
pydantic-settings>=2.2.0

# AI — using Gemini REST API via httpx (avoids grpcio DLL issues on Windows)
# google-generativeai is NOT needed; we call the REST endpoint directly

# PDF generation
reportlab>=4.1.0

# HTTP client (for async external calls)
httpx>=0.27.0

# Email validation
email-validator>=2.1.0
